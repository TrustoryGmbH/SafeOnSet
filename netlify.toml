[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "functions"

[functions."weekly-report"]
  schedule = "0 6 * * 1" # Jede Woche Montag um 6:00 AM

# SPA Fallback: Alle Routen auf index.html umleiten
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
